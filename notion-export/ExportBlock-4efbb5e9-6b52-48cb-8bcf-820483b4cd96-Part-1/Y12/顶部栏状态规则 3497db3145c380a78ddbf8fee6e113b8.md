# 顶部栏状态规则

### **游客状态**

顶部栏右侧入口显示“登录”。

点击“登录”按钮后，打开登录弹窗。

不展示个人中心下拉菜单。

### **登录状态**

顶部栏右侧入口显示“个人中心”。

点击“个人中心”后，展示个人中心下拉框。

个人中心下拉框包含：

- 观看历史
- 我的点赞
- 退出账号

### **异常场景**

如果用户状态读取失败，默认展示“登录”。

如果本地存储损坏或字段缺失，清空异常状态并按游客状态处理。

如果登录态已失效但本地仍保留旧状态，前端刷新后应重新按最新有效状态渲染，当前静态版本可先按本地状态为准。